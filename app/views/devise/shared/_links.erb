<div class="row shared-links">
  <div class="col-sm-8">
    <%- if controller_name != 'sessions' %>
    <%= link_to "Identificarse", new_session_path(resource_name) %><br />
    <% end -%>

    <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
    <%= link_to "Olvidaste tu clave?", new_password_path(resource_name) %><br />
    <% end -%>

    

    <%- if devise_mapping.omniauthable? %>
    <%- resource_class.omniauth_providers.each do |provider| %>
    <%= link_to "Logearse con #{provider.to_s.titleize}", omniauth_authorize_path(resource_name, provider) %><br />
    <% end -%>
    <% end -%>
  </div>
</div>
