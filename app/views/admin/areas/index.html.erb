<div class='row'>
  <div class='columns large-12 small-12'>
    <p id="notice"><%= notice %></p>
    <dl class='sub-nav'>
      <dt>Areas</dt>
      <dd><%=link_to "ASIGNACIÓN DE USUARIOS", asignacion_admin_areas_path %></dd>
    </dl>
    <h1>Areas</h1>

    <table>
      <thead>
        <tr>
          <th>Nombre</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @areas.each do |area| %>
          <tr>
            <td><%= area.nombre %></td>
            <td><%= link_to 'Mostrar', admin_area_path(area) %></td>
            <td><%= link_to 'Editar', edit_admin_area_path(area) %></td>
            <td><%= link_to 'Eliminar', [:admin, area], method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'Nueva Area', new_admin_area_path %>
  </div>
</div>